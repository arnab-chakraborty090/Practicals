#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n,i,j,count=0;
    printf("Enter the last number:");
    scanf("%d",&n);

    if(n<=1)
        printf("There are no prime numbers");
    else
    {
        printf("Prime numbers upto %d :\n",n);
        for(i=2;i<=n;i++)
        {
            for(j=1;j<=i;j++)
            {
                if(i%j==0)
                count++;
            }
            if(count==2)
            printf("%d\n",i);
            count=0;
        }
    }
    return 0;
}
